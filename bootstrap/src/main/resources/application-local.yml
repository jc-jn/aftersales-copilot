spring:
  flyway:
    locations: classpath:db/migration/structure,classpath:db/migration/demo
    out-of-order: true

app:
  security:
    jwt-secret: ${JWT_SECRET:local-only-jwt-secret-change-before-deployment-2026}
